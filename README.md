# sarthaktiwari.xyz

Personal blog. Built with Jekyll, hosted on GitHub Pages.

## Run locally

```bash
bundle install
bundle exec jekyll serve --livereload
```

Open http://localhost:4000.

## Deploy

Merging to `master` triggers the CI workflow which builds and pushes to `gh-pages`.
